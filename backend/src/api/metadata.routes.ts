import { Router } from 'express'

const router = Router()

// TODO: Implement metadata routes
// - GET /campaigns/:campaignId
// - POST /campaigns/:campaignId
// - PUT /campaigns/:campaignId/:cardId
// - DELETE /campaigns/:campaignId/:cardId

export default router
