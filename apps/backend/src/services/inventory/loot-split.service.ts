/**
 * Loot Split Service — deprecated, kept empty for import compatibility.
 * /loot-split now applies splits immediately in the command handler.
 * This file can be deleted once all imports are cleaned up.
 */
